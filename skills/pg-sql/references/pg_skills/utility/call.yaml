skillid: pg_call
name: CALL
category: Utility

description:
  brief: "调用一个过程"

syntax:
  primary: "CALL name ( [ argument ] [, ...] )"

key_parameters:
  - name: "name"
    description: "过程的名称（可选地模式限定）"
  - name: "argument"
    description: "过程调用的参数表达式"

related_commands:
  - "CREATE PROCEDURE"

metadata:
  source_page: 1640